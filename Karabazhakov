 for(int i = 0; i < size; ++i)
    {
        for(int j = 0; j < size; ++j)
        {
            if(mainMatrix[i][j] == 1)
            {
                cout << i + 1 << " " << j + 1 << "\n";
                mainMatrix[j][i] = 0;
            }
        }
    }
